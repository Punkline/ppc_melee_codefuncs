.include "melee"
melee arch, MPad, GObj
punkpc ppc   # import punkpc/melee symbols and macros

arch.start
# Start a new archive wrapper
# - everything between this line and 'arch.end' is part of the data section

# The following 'list' section lists

arch.symbol "list"
0:
.long (0b-1f)>>2
# pointer list starts with a pointer count

arch.point _onSync, _gproc, _colors
1:  # pointers are listed with arch.point, so that they can be relocated



arch.symbol "ppc"
# start of ppc text section of archive file ...

_onSync:
  prolog


_gproc:
  prolog rData, rFog, rColors, rIndex, rPad, rPressing, rHolding
  # prologue for GObj process function backs up these as saved registers

  lwz r3, r13.xPLinks(r13)
  lwz r3, PLink.xStageLight(r3)
  lwz rFog, GObj.xObj(r3)
  # Fog object in rFog

  load rPad, MPad.base






1.02 ------ 801a1c2c --- 48001a75 -> Branch
# on title screen update
